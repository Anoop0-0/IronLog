# Base URL of the IRONLOG API (no trailing slash)
VITE_API_URL=http://localhost:5000/api

# Google OAuth client ID (from Google Cloud Console) — must match the
# server's GOOGLE_CLIENT_ID
VITE_GOOGLE_CLIENT_ID=
